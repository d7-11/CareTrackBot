# 🟢 Офіційний Node.js образ
FROM node:18-alpine

# 🔧 Встановлюємо системну бібліотеку, якої бракує
RUN apk add --no-cache libatomic

# 📂 Встановлюємо робочу директорію
WORKDIR /app

# 📦 Копіюємо package.json і встановлюємо залежності
COPY package*.json ./
RUN npm install --production

# 📁 Копіюємо решту файлів
COPY . .

# ▶️ Команда для запуску бота
CMD ["node", "index.js"]
