{
  "name": "caretrack",
  "version": "1.0.0",
  "description": "Telegram бот для відстеження прийому ліків",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "migrate": "npx dotenv sequelize-cli db:migrate",
    "migrate:undo": "npx dotenv sequelize-cli db:migrate:undo",
    "migrate:status": "npx dotenv sequelize-cli db:migrate:status"
  },
  "keywords": ["telegram", "bot", "health", "medications"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "pg": "^8.16.3",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "sequelize-cli": "^6.6.3"
  }
}